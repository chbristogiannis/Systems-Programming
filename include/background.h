#pragma once

#include "common.h"
#include <stdbool.h>

// This function backgroundComAnalysis takes a command array and the number of commands in it, numOfCom
// breaks each command and execute it sepertly
void backgroundComAnalysis(char** , int );